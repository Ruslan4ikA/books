# 📚 Django-приложение: Управление книгами

Современное веб-приложение на Django с поддержкой **PostgreSQL**, **Docker**, **AJAX-поиском** и **CRUD-операциями**. Позволяет управлять книгами через веб-интерфейс и загружать данные из файлов.

---

## 🌟 Возможности

- ✅ Добавление книг через веб-форму с выбором хранилища: **файл (JSON)** или **база данных (PostgreSQL)**
- ✅ Просмотр данных из **файлов** или **базы данных** (переключение на главной странице)
- ✅ **AJAX-поиск** по названию и автору (для записей в БД)
- ✅ **Редактирование и удаление** книг из БД с валидацией
- ✅ **Проверка на дубликаты** при сохранении в БД или файл
- ✅ Строгая валидация:
  - Обязательные поля: `title`, `year`
  - Год: число от 0 до 2025
- ✅ Совместимость: старые записи без новых полей отображаются с прочерком `"—"`
- ✅ Безопасная загрузка файлов:
  - Имена генерируются автоматически
  - Невалидные файлы удаляются с сообщением об ошибке
- ✅ Производственная сборка с **Gunicorn + Nginx**

---

## 🧰 Технологии

- Python 3.11
- Django 5.2
- PostgreSQL 15
- Docker + Docker Compose
- Gunicorn (WSGI-сервер)
- Nginx (reverse proxy, статика)
- Bootstrap 5 (UI)

---

## 🐳 Запуск через Docker (рекомендуется)

### 📥 1. Клонируйте репозиторий

```bash
git clone https://github.com/Ruslan4ikA/book_project.git
cd book_project
cp .env.example .env
```
Отредактируйте .env:
```
SECRET_KEY=ваш_уникальный_секретный_ключ
DEBUG=0
POSTGRES_DB=books_db
POSTGRES_USER=bookuser
POSTGRES_PASSWORD=надёжный_пароль
```
Запустите приложение
```bash
docker-compose up -d --build
```
Приложение будет доступно по адресу:
👉 http://localhost

---
🔄 Миграция данных из SQLite в PostgreSQL
Если у вас есть старая база db.sqlite3:

1. Поместите файл в корень проекта
```commandline
book_project/
└── db.sqlite3   ← ваша старая база
```
2. Скопируйте скрипт и базу в контейнер

```
docker cp scripts/migrate_sqlite_to_postgres.py book_project-web-1:/app/
docker cp db.sqlite3 book_project-web-1:/app/
```
3. Запустите миграцию

```commandline
docker-compose exec web python migrate_sqlite_to_postgres.py
```
Скрипт:

- Подключится к SQLite
- Выполнит перенос всех книг в PostgreSQL
- Автоматически обработает дубликаты
- Покажет статистику в консоли

---

✨ Автор: Ruslan
📅 2025
🎓 Учебный проект по Web и Python 